import React from 'react';
import { Outlet, Link } from 'react-router-dom';

const NatalLayout = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default NatalLayout;
